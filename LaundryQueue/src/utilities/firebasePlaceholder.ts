// Placeholder module for Firebase integration. Replace with real config and init.
export const firebaseConfig = {
  apiKey: '<YOUR_API_KEY>',
  authDomain: '<YOUR_AUTH_DOMAIN>',
  databaseURL: '<YOUR_DATABASE_URL>',
  projectId: '<YOUR_PROJECT_ID>',
};

export const initFirebase = async () => {
  // Intentionally empty; guide to add Firebase SDK here.
  console.log('initFirebase: replace with real Firebase initialization');
};

export default { firebaseConfig, initFirebase };
